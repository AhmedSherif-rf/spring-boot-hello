pipeline {
  agent any
  
  stages {
    stage('Build Docker Image') {
      steps {
        script {
         docker.withVersion('1.39') {
            sh 'docker version' }
          def dockerImage = docker.build('my-hello-spring')
        }
      }
    }
  }
}
