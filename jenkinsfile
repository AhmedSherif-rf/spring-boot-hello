pipeline {
  agent any

  stages {
    stage('Docker version') {
      steps {
        script {
          docker.withVersion('1.39') {
            sh 'docker version'
          }
        }
      }
    }
  }
}
